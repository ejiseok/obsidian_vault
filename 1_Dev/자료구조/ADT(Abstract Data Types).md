
- **자료구조가 갖춰야 할 일련의 연산**

- 리스트를 예로 들면
	- 리스트는 데이터에 순차적으로 접근해서 그 데이터를 다룰 수 있는 기능을 제공해야 한다.
	- 특정 위치의 노드에 접근(Get), 목록 마지막에 데이터를 추가(Append), 중간에 삽입(Insert), 삭제(Remove)하는 기능들처럼

- ADT가 청사진을 제공하면 [[자료구조(Data Structure)|자료구조]]는 이를 구현하는 형태다.

![[Pasted image 20251107184642.png]]
